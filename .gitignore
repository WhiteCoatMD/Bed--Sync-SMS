node_modules/
.next/
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.vercel

# Claude Code local settings (contain machine-local secrets — never commit)
.claude/settings.local.json

# Env files with secrets (never commit)
.env.sms-production
.env.pull-temp
.env.telnyx
